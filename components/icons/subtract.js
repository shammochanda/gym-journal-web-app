import { Fragment } from "react";
import classes from './subtract.module.css'

const Subtract = (props) => {

  return (
    <Fragment>
      <div className={classes.bar1}></div>
    </Fragment>
  );
};

export default Subtract;
